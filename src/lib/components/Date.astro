---
import { formatDate } from "$lib/utils";

interface Props {
  date: Date | string;
  text?: string;
}

const { date, text } = Astro.props;
---

{text && <span set:text={text + " "} />}
<time datetime={String(date)}>{formatDate(date)}</time>
